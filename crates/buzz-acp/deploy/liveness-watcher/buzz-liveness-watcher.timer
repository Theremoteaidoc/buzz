[Unit]
Description=Periodic Buzz agent liveness probe (WO #135)

[Timer]
# Poll well inside dead_after (225s) so a wedge surfaces within one window.
OnBootSec=30s
OnUnitActiveSec=60s
AccuracySec=5s
Unit=buzz-liveness-watcher.service

[Install]
WantedBy=timers.target
